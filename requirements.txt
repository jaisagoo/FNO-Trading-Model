# Core runtime (classical mode - no GPU / torch required)
numpy>=1.26.0
scipy>=1.12.0
pandas>=2.2.0
matplotlib>=3.8.0
yfinance>=0.2.36
pyarrow>=15.0.0

# FNO mode:   pip install -e ".[fno]"       (adds torch)
# Dev tools:  pip install -e ".[dev]"       (pytest, black, ruff, mypy)
# Notebooks:  pip install -e ".[notebooks]" (jupyter, ipykernel)
